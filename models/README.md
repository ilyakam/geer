# Model Repositories

Geer's model weights are distributed through dedicated Hugging Face model
repositories, not through this GitHub repository.

Entries below this directory are optional, non-updating Git submodules for
provenance and maintainer navigation. Geer's installer and runtime use the
revision-pinned Hugging Face API and never read model weights from a submodule
checkout.
