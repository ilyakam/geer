# Model Repositories

Geer's model weights are distributed through dedicated Hugging Face model
repositories, not through this GitHub repository.

No model submodules are tracked here. Geer's installer and runtime use the
revision-pinned Hugging Face API and never read model weights from a Git
checkout.
